model Contact {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  phone     String?
  company   String?
  subject   String
  message   String
  createdAt DateTime @default(now())

  @@map("contact")
}
